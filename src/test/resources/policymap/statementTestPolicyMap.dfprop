map:{
    ; actionExceptList = list:{}
    ; actionTargetList = list:{}
    ; fieldExceptMap = map:{}
    ; methodMap = map:{
        ; themeList = list:{}
        ; statementList = list:{}
    }
    ; fieldMap = map:{
        ; themeList = list:{}
        ; statementList = list:{
            ; if fieldName is suffix:Str then bad
            ; if fieldName is suffix:Id then fieldType is Integer or Long
        }
    }
}